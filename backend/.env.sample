GOOGLE_API_KEY=

# MySQL (local install). Create the database and user first, e.g.
#   CREATE DATABASE auto_brochure_rag CHARACTER SET utf8mb4;
DATABASE_URL=mysql+aiomysql://root:password@localhost:3306/auto_brochure_rag

# Qdrant (docker compose up -d qdrant)
QDRANT_URL=http://localhost:6333
